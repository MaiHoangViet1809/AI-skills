interface:
  display_name: "TaskProgressReport"
  short_description: "Summarize execution progress or subject-specific inventory"
  default_prompt: "Use $task-progress-report to choose inventory columns or an execution progress table from the requested subject and available evidence."
