interface:
  display_name: "TaskReviewInvestigateCompare"
  short_description: "Review evidence and write concrete findings back into known plans by default"
  default_prompt: "Use TaskReviewInvestigateCompare to review a plan, SOW, request, or idea. Write concrete, low-ambiguity findings back into a known existing plan or SOW by default unless the user requests discussion only; route material scope changes separately."
