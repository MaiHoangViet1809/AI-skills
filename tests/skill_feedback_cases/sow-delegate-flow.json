{
  "schema_version": 1,
  "cases": [
    {
      "id": "native-glm-delegation-001",
      "target_skill": "sow-delegate-flow",
      "cause_class": "decision_procedure",
      "feedback": {
        "trigger": "A user asks to delegate a task, SOW, or plan to GLM5.2 after adding that native Codex sub-agent.",
        "actual_behavior": "The skill routes work through a Claude CLI session, logs, and parser instead of the requested native sub-agent.",
        "expected_behavior": "Select the registered GLM5.2 native sub-agent, preserve SOW-first controls, and independently review the result.",
        "invariant": "Explicit delegate-model requests use the requested available native sub-agent without a silent provider fallback.",
        "evidence_ref": "sanitized:user-feedback-native-glm-delegation-2026-08-21"
      },
      "scenarios": [
        {
          "kind": "expected",
          "request": "Delegate this approved SOW to GLM5.2.",
          "context": "The SOW has explicit write scope and verification criteria; GLM5.2 is registered as a native sub-agent.",
          "expected_behavior": "Spawn the GLM5.2 sub-agent with the SOW path and bounded ownership, then review and verify locally.",
          "must_not": "Invoke Claude CLI, create Claude logs, or substitute another model."
        },
        {
          "kind": "negative",
          "request": "Implement this approved SOW locally.",
          "context": "The user does not request delegation or name a sub-agent.",
          "expected_behavior": "Use the normal execution flow without spawning a delegate.",
          "must_not": "Activate delegation merely because a SOW exists."
        },
        {
          "kind": "boundary",
          "request": "Delegate this draft plan to GLM5.2.",
          "context": "The plan is not approved and the request is for investigation and SOW drafting only.",
          "expected_behavior": "Allow scoped inspection or plan drafting, but prohibit product-code changes until an approved SOW exists.",
          "must_not": "Authorize implementation work from the unapproved plan."
        }
      ]
    },
    {
      "id": "native-delegate-closeout-gates-001",
      "target_skill": "sow-delegate-flow",
      "cause_class": "decision_procedure",
      "feedback": {
        "trigger": "A native sub-agent completes delegated work and the coordinator decides whether the task can close.",
        "actual_behavior": "The coordinator can accept one local check, while approval wording conflicts with pre-approval planning delegation and the GLM role is treated as globally fixed.",
        "expected_behavior": "Gate only implementation on SOW approval, resolve the requested agent from the current catalog, and independently complete runtime verification plus gap-finding.",
        "invariant": "Native delegation preserves model intent, authorization boundaries, and the same verification standard as local execution.",
        "evidence_ref": "sanitized:final-review-native-delegation-2026-08-21"
      },
      "scenarios": [
        {
          "kind": "expected",
          "request": "Delegate this approved async UI SOW to GLM5.2 and finish it.",
          "context": "The current agent catalog exposes a GLM5.2 custom role and the delegate reports implementation complete.",
          "expected_behavior": "Spawn the catalog-resolved GLM5.2 role, then require a multi-step runtime scenario, evidence against the SOW lock, and a gap-finding pass before closeout.",
          "must_not": "Treat delegate completion, syntax checks, or one generic local check as task completion."
        },
        {
          "kind": "negative",
          "request": "Delegate investigation and draft a SOW for this issue.",
          "context": "No approved implementation SOW exists and the delegated scope is read-only analysis plus planning documentation.",
          "expected_behavior": "Allow the planning delegation while prohibiting implementation writes.",
          "must_not": "Reject all delegation before approval or authorize product-code changes."
        },
        {
          "kind": "boundary",
          "request": "Delegate this approved SOW to GLM5.2.",
          "context": "GLM5.2 is requested but its previously known role name is absent from the current agent catalog.",
          "expected_behavior": "Resolve any currently registered GLM5.2 role; if none exists, report unavailability without substitution.",
          "must_not": "Use a stale hardcoded role or silently delegate to another model."
        }
      ]
    },
    {
      "id": "portable-delegate-rule-001",
      "target_skill": "sow-delegate-flow",
      "cause_class": "decision_procedure",
      "feedback": {
        "trigger": "Delegate a read-only review to an available agent.",
        "actual_behavior": "The entrypoint references an external repository rule file absent from skill-only installs.",
        "expected_behavior": "Delegate communication instructions are self-contained.",
        "invariant": "Delegate communication instructions are self-contained.",
        "evidence_ref": "sanitized:sow-0082-source-review-2026-09-15"
      },
      "scenarios": [
        {
          "kind": "expected",
          "request": "Delegate a read-only review to an available agent.",
          "context": "Only the delegate skill directory is installed.",
          "expected_behavior": "Use packaged instructions and concise communication.",
          "must_not": "Depend on an external brief-execution file."
        },
        {
          "kind": "negative",
          "request": "Read the delegation instructions.",
          "context": "No historical rules folder exists.",
          "expected_behavior": "Explain the bounded handoff without missing-file dependencies.",
          "must_not": "Install legacy rules."
        },
        {
          "kind": "boundary",
          "request": "Delegate from an arbitrary skills root.",
          "context": "Project policy exists separately.",
          "expected_behavior": "Use local skill resources and respect project policy.",
          "must_not": "Overwrite project policy."
        }
      ]
    },
    {
      "id": "custom-agent-fresh-session-001",
      "target_skill": "sow-delegate-flow",
      "cause_class": "decision_procedure",
      "feedback": {
        "trigger": "A user asks to delegate a task to a custom agent that is not exposed as a native Codex sub-agent.",
        "actual_behavior": "The delegate flow only defines the native Codex lifecycle and does not require a new external provider session, so an old task conversation or session could be reused.",
        "expected_behavior": "Classify transport first; native agents use the native lifecycle, while every new logical task delegated to a non-native custom agent starts a fresh provider or CLI session with no old task history.",
        "invariant": "Non-native custom delegation never reuses a prior task session or historical chat; same-task continuation is explicitly bounded to the fresh session created for that task.",
        "evidence_ref": "sanitized:user-feedback-custom-agent-fresh-session-2026-09-18"
      },
      "scenarios": [
        {
          "kind": "expected",
          "request": "Delegate this approved SOW to an external custom agent through its CLI.",
          "context": "The agent is not in the native Codex catalog and the provider documents a new-session mechanism.",
          "expected_behavior": "Initialize a unique fresh provider session, send a self-contained bounded prompt, and return session lifecycle plus verification evidence to the coordinator.",
          "must_not": "Resume or continue an old task session, attach historical chat, or pass a prior transcript."
        },
        {
          "kind": "negative",
          "request": "Delegate this approved SOW to native GLM5.2.",
          "context": "The current native agent catalog exposes the registered GLM5.2 role.",
          "expected_behavior": "Use the native `spawn_agent` lifecycle and keep the coordinator's independent review gates.",
          "must_not": "Create an external CLI session or treat a native child as a non-native agent."
        },
        {
          "kind": "boundary",
          "request": "Send a follow-up after the external custom agent asks a question.",
          "context": "The follow-up belongs to the same logical task and the just-created fresh session identity remains provable.",
          "expected_behavior": "Continue only that fresh task session with the minimum approved clarification; if history is compacted, stale, or uncertain, start another fresh session with a concise handoff.",
          "must_not": "Silently attach an older task conversation or reuse a session whose task identity cannot be proved."
        }
      ]
    }
  ]
}
