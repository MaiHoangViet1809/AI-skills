interface:
  display_name: "SowDelegateFlow"
  short_description: "SOW-first native sub-agent delegation"
  default_prompt: "Use $sow-delegate-flow to delegate this approved SOW to GLM5.2, then independently review and verify the result."
